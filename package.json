{
  "name": "spotify-music-videos",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",
    "express": "^4.16.3",
    "googleapis": "^30.0.0",
    "request": "^2.85.0"
  },
  "scripts": {
    "start": "node build/index.js",
    "heroku-postbuild": "cd client && yarn && yarn run build",
    "build": "NODE_ENV=development babel index.js config.js -d build --presets es2015",
    "server": "yarn build && yarn start"
  }
}
